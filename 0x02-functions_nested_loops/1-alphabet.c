#include "main.h"
/**
  * void print_alphabet(void) - prints alphabet in lower case
  *
  *Return: always 0;
  */

void print_alphabet(void)
{
	char i;

	for (i = "a" ; 1 <= 'z' ; i++)
		_putchar(i);
	_putchar('\n');

	return = (0)
}
